import { LandingV4 } from './landing'

/**
 * Page d'accueil (landing publique). Le contenu est la landing « v4 (moderne) »,
 * découpée en sections dans `src/pages/landing/`.
 */
export function HomePage() {
  return <LandingV4 />
}

export default HomePage
